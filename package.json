{
  "name": "lost-found-worker",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "favicon": "node scripts/make-favicon.mjs"
  },
  "devDependencies": {
    "wrangler": "^4.129.1"
  }
}
